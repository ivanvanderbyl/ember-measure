import getContentRect from 'dummy/utils/get-content-rect';
import { module, test } from 'qunit';

module('Unit | Utility | get content rect');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getContentRect();
  assert.ok(result);
});
